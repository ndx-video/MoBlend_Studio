---
file: M000.008.progress-yaml-frontmatter.md
date: 2026-06-14
milestone: M0 — Specs & Scaffold
related_commits: uncommitted
---

# M0 — YAML frontmatter for progress entries

## Summary

Replaced the bullet-list metadata block at the top of each `.progress/` entry (`**File:**`, `**Date:**`, etc.) with YAML frontmatter. The H1 title now sits immediately below the closing `---`. Retrofitted all existing `M000.001`–`M000.007` entries and updated the entry template in `.progress/README.md`.

## Changes

- **`.progress/README.md`** — entry template now uses frontmatter; note about milestone prose references points at the `milestone` field.
- **`.progress/M000.001`–`M000.007`** — metadata moved to frontmatter (`file`, `date`, `milestone`, `related_commits`).

## Decisions

- Frontmatter field names are lowercase with underscores (`related_commits`, not `Related commits`).
- `milestone` keeps the ROADMAP short form (`M0 — Specs & Scaffold`), same as before.
- Body sections (`## Summary`, `## Changes`, …) are unchanged.

## References

- [.progress/README.md](README.md)
- [M000.003.progress-system.md](M000.003.progress-system.md)
