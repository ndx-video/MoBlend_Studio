---
file: M002.002.m2-committed.md
date: 2026-06-14
milestone: M2 — API Broker
related_commits: 1856deb
---

# M2 — Closed in git

## Summary

M2 API Broker landed in git as commit `1856deb`. Pre-commit audit fixes applied: concurrent WS reader/processor so `_latest_wanted_frame` updates during in-flight renders (post-render stale drop now correct); `asyncio.to_thread` for non-blocking WS waits; one-time EEVEE preview tuning in `render_frame`; main-thread queue polling in `serve_forever` (headless `--background` mode); M1 template camera for renders; `engine/vendor` pip target in `dev.ps1`; automated M2 client with steady-state fps gate (≥12 fps headless baseline), scrub stale assertions, and FRAME header unpack fix. Full `scripts/dev.ps1` (M1 + broker + M2 client) verified exit 0 on Blender 5.1.2.

## Changes

- Commit `1856deb`: broker (`server.py`), `render_frame`, bootstrap `--serve`, dev.ps1 M1/M2 flow, verification clients, M002.001
- Audit fixes bundled: WS reader/worker split, queue drain loop, EEVEE preview config, vendor deps install, m2 client hardening, m1_roundtrip camera

## Decisions

- M2 milestone complete; M3 (Wails MVP) is next per ROADMAP.
- M002.001 left immutable; this entry records the landing commit and audit fixes.
- Automated fps gate uses ≥12 fps steady-state on 256×144 headless EEVEE (margin below PRD 30 fps target for dev/CI hardware).

## Follow-ups

- M3: Wails shell spawns/supervises Blender; React connects directly to broker REST + WS.
- Optional: disable WS permessage-deflate; explicit 501 stubs for render/export.

## References

- [M002.001.api-broker-m2.md](M002.001.api-broker-m2.md)
- [M001.002.m1-committed.md](M001.002.m1-committed.md)
- [ROADMAP.md](../ROADMAP.md)
- Commit: `1856deb`
