"""Mo.Blend Engine (M0 scaffold).

This package runs inside Blender's bundled Python via `blender --python`.
Core logic (manifest, parameters, timeline, assets) will be added in M1+.
"""

__version__ = "0.0.0"
__all__ = ["__version__"]
